package cgresearch.studentprojects.brickbuilder.brickcloud;

/**
 * Possible brick rotations
 */
public enum BrickRotation {
	XDIR_POS, // x-axis positive direction (left to right)
	XDIR_NEG, // 
	ZDIR_POS, // z-axis positive direction (back to front)
	ZDIR_NEG  //
}